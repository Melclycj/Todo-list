# Project Todo

## Tasks
- [x] Decide the frontend design for each component — design plan locked in, full frontend scaffold built
- [ ] Create a new cmd/skill for initialising a project: 1) add .gitattributes
- [x] Wire up frontend to backend — Vite dev proxy to localhost:8000
- [x] Add Nginx config and Dockerfile for frontend — multi-stage Docker build
- [x] Add frontend to docker-compose — production + dev overrides
- [x] E2E tests — Playwright, 5 flows: auth, task CRUD, status, topics, recurring
